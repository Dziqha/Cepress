---
import DocsLayout from '../layouts/DocsLayout.astro';
import FeatureCard from '../components/FeatureCard.astro';
import CodeBlock from '../components/CodeBlock.astro';
---

<DocsLayout title="Introduction" description="Cepress Generator - Create Node.js Express projects with modern tools in seconds">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Hero Section -->
    <section class="text-center mb-16">
      <div class="flex flex-col items-center justify-center space-y-4 mb-8">
        <img src="/cepress.png" alt="Cepress Logo" class="w-20 h-16 sm:w-24 sm:h-20" />
        <div>
          <h1 class="text-3xl sm:text-4xl lg:text-5xl font-bold text-gray-900 dark:text-white mb-2">
            Cepress Generator
          </h1>
          <div class="flex flex-wrap items-center justify-center gap-2">
            <span class="px-2 py-1 rounded-full text-xs font-medium bg-primary-100 text-primary-800 dark:bg-primary-900 dark:text-primary-200">
              v1.0.2
            </span>
            <span class="px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-200">
              Open Source
            </span>
          </div>
        </div>
      </div>

      <p class="text-lg sm:text-xl text-gray-600 dark:text-gray-300 max-w-3xl mx-auto leading-relaxed mb-8">
        Create production-ready Node.js Express applications with modern tools and best practices in seconds.
        Configure databases, authentication, validation, and API documentation with an interactive CLI.
      </p>

      <div class="flex flex-col sm:flex-row items-center justify-center space-y-3 sm:space-y-0 sm:space-x-4 mb-12 w-full">
        <a href="/quick-start" class="w-full sm:w-auto inline-flex items-center justify-center px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-semibold rounded-lg transition-colors">
          <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
          Get Started
        </a>
        <a href="https://github.com/Dziqha/CepressDocs" target="_blank" class="w-full sm:w-auto inline-flex items-center justify-center px-6 py-3 bg-gray-100 hover:bg-gray-200 dark:bg-gray-800 dark:hover:bg-gray-700 text-gray-900 dark:text-white font-semibold rounded-lg transition-colors">
          <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 24 24">
            <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
          </svg>
          GitHub
        </a>
      </div>
    </section>

    <!-- Command Preview -->
    <div class="mb-16 w-full max-w-fit mx-auto overflow-x-auto rounded-lg bg-gray-800 border border-gray-700 px-4 py-2 flex items-center gap-3">
      <code class="text-sm font-mono text-gray-200 whitespace-nowrap">npx create-cepress-app my-app</code>
      <button
        class="text-gray-200 :text-white transition p-1"
        onclick="navigator.clipboard.writeText('npx create-cepress-app my-app')"
        title="Copied!"
      >
        <svg class="w-4 h-4" viewBox="0 0 24 24" fill="currentColor">
          <path d="M7 6V3a1 1 0 0 1 1-1h12a1 1 0 0 1 1 1v14a1 1 0 0 1-1 1h-3v3c0 .552-.45 1-1.007 1H4.007A1.001 1.001 0 0 1 3 21l.003-14c0-.552.45-1 1.006-1zM5.002 8L5 20h10V8zM9 6h8v10h2V4H9z"/>
        </svg>
      </button>
    </div>
    

    <!-- Key Features -->
    <section class="mb-16">
      <div class="text-center mb-12">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
          Everything You Need to Build Modern APIs
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
          Cepress Generator includes all the essential tools and configurations for building production-ready Express.js applications.
        </p>
      </div>
      
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
        <FeatureCard icon="🗄️" title="Multiple Database Support" description="Choose from SQLite, PostgreSQL, or MySQL with optional Prisma ORM integration for type-safe database operations." />
        <FeatureCard icon="🔐" title="Built-in Authentication" description="JWT-based authentication system with password hashing, user registration, and login endpoints ready to use." />
        <FeatureCard icon="📚" title="API Documentation" description="Automatic Swagger/OpenAPI 3.0 documentation generation with interactive API explorer using swagger-ui-express." />
        <FeatureCard icon="✅" title="Request Validation" description="Robust input validation using Zod schemas with automatic error handling and type inference." />
        <FeatureCard icon="🏗️" title="Project Templates" description="Pre-built model relationships (User + Post) or start with a clean slate - perfect for rapid prototyping." />
        <FeatureCard icon="⚡" title="Modern Tooling" description="ESM modules, environment configuration, error handling, CORS setup, and development server with hot reload." />
      </div>
    </section>

    <!-- Project Structure -->
    <section class="mb-16">
      <div class="bg-white dark:bg-gray-800 rounded-xl border border-gray-200 dark:border-gray-700 p-6 shadow-lg overflow-x-auto">
        <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-6 flex items-center">
          <svg class="w-5 h-5 mr-3 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"></path>
          </svg>
          Generated Project Structure
        </h3>
        <CodeBlock 
          language="text"
          code={`my-cepress-app/
├── src/
│   ├── controllers/     # Route handlers & business logic
│   ├── middleware/      # Custom middleware functions
│   ├── models/          # Database models (Sequelize/Prisma)
│   ├── routes/          # API route definitions
│   ├── schemas/         # Zod validation schemas
│   ├── config/          # Database & app configuration
│   ├── utils/           # Helper functions & utilities
│   ├── app.js           # Express app setup & middleware
│   └── server.js        # Application entry point
├── prisma/              # Prisma schema (if selected)
│   └── schema.prisma    # Database schema definition
├── .env.example         # Environment variables template
├── .gitignore           # Git ignore rules
├── package.json         # Dependencies & scripts
└── README.md            # Project documentation`} 
        />
      </div>
    </section>

    <!-- How It Works -->
    <section class="mb-16">
      <div class="text-center mb-12">
        <h2 class="text-2xl sm:text-3xl font-bold text-gray-900 dark:text-white mb-4">
          Ready in Minutes, Not Hours
        </h2>
        <p class="text-lg text-gray-600 dark:text-gray-300 max-w-2xl mx-auto">
          Our interactive CLI guides you through the setup process, generating a fully configured project tailored to your needs.
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        {[1, 2, 3].map(step => (
          <div class="text-center">
            <div class="w-16 h-16 bg-primary-100 dark:bg-primary-900 rounded-xl flex items-center justify-center mx-auto mb-6">
              <span class="text-2xl font-bold text-primary-600 dark:text-primary-400">{step}</span>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-3">
              {step === 1 ? 'Install & Configure' : step === 2 ? 'Setup Environment' : 'Start Building'}
            </h3>
            <p class="text-gray-600 dark:text-gray-300 leading-relaxed">
              {step === 1 && 'Run the command and answer a few questions about your project preferences.'}
              {step === 2 && 'Configure your database connection and environment variables.'}
              {step === 3 && 'Your Express API is ready with authentication, validation, documentation, and best practices built-in.'}
            </p>
          </div>
        ))}
      </div>
    </section>

    <!-- Technology Stack -->
    <section class="mb-16">
      <div class="bg-gray-50 dark:bg-gray-800/50 rounded-xl p-8">
        <h3 class="text-xl font-bold text-gray-900 dark:text-white mb-6 text-center">
          Built with Modern Technologies
        </h3>
        <div class="grid grid-cols-3 sm:grid-cols-6 gap-6">
          {[
            ['N', 'Node.js', 'bg-green-500'],
            ['E', 'Express', 'bg-gray-800'],
            ['P', 'Prisma', 'bg-blue-600'],
            ['Z', 'Zod', 'bg-purple-600'],
            ['S', 'Swagger', 'bg-orange-500'],
            ['J', 'JWT', 'bg-red-500'],
          ].map(([initial, name, bg]) => (
            <div class="text-center">
              <div class={`w-12 h-12 ${bg} rounded-lg flex items-center justify-center mx-auto mb-2`}>
                <span class="text-white font-bold">{initial}</span>
              </div>
              <span class="text-sm font-medium text-gray-700 dark:text-gray-300">{name}</span>
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="text-center">
      <div class="bg-primary-50 dark:bg-primary-900/20 border border-primary-200 dark:border-primary-800 rounded-xl p-8">
        <div class="flex items-center justify-center mb-4">
          <span class="text-3xl mr-3">🚀</span>
          <h3 class="text-xl font-bold text-primary-900 dark:text-primary-100">Ready to Build Something Amazing?</h3>
        </div>
        <p class="text-primary-700 dark:text-primary-300 mb-6 max-w-2xl mx-auto">
          Join thousands of developers who are building faster with Cepress Generator.
        </p>
        <div class="flex flex-col sm:flex-row items-center justify-center space-y-3 sm:space-y-0 sm:space-x-4">
          <a href="/installation" class="w-full sm:w-auto inline-flex items-center justify-center px-6 py-3 bg-primary-600 hover:bg-primary-700 text-white font-semibold rounded-lg transition-colors">
            Start Building Now
            <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
          <a href="/quick-start" class="w-full sm:w-auto inline-flex items-center justify-center px-6 py-3 text-primary-600 dark:text-primary-400 font-semibold hover:text-primary-700 dark:hover:text-primary-300 transition-colors">
            View Quick Start Guide
          </a>
        </div>
      </div>
    </section>
  </div>
</DocsLayout>
